# Source baseline verification — September 26, 2026

Tested revision: `b1bb742fd6e4355486e15f9070a85cbc1bd9772d` on protected main.
This is a pinned verification baseline, **not an approved or published release**.
Later documentation or implementation commits are not covered by these local
results. A final release candidate needs its own exact revision review.

## Environment and isolation

A new detached Git worktree at that SHA had a clean `git status --porcelain`.
A fresh `.venv-ci` was created in it; no existing development environment, native
helper build, model service, private calibration, camera, or robot was used.
This was a clean source checkout sharing Git objects with the existing clone,
not an independent network-clone test or a clean OS image.

Host: Windows, platform string `Windows-10-10.0.26200-SP0`; Python 3.10.10.
The base install encountered DNS retries at the host's configured secondary
NVIDIA index but completed through PyPI. For the test-extra install only,
`PIP_EXTRA_INDEX_URL=https://pypi.org/simple` was set in the command environment
to avoid those retries. No persistent pip configuration was changed. Installation
used network access and could reuse pip's download cache.

## Commands and results

Run from the pinned checkout root:

```text
python -m venv .venv-ci
python scripts/ci/offline_checks.py install-base
python scripts/ci/offline_checks.py smoke
python scripts/ci/check_docs.py
python scripts/ci/offline_checks.py install-tests
python scripts/ci/offline_checks.py test
```

- Base and test-extra installs: PASS; both `pip check` invocations reported no
  broken requirements. Package installed non-editably as `rocell` 0.1.0.
- Smoke: PASS; isolated installed-package import, ordered H/I targets, nominal
  geometry, no controller commands and no execution authority.
- Documentation: PASS; local file links in 18 maintained docs and two SVG XML
  files. No claim about remote links, anchors, or visual rendering.
- Portable selection: **122 passed**, 7.67 seconds.

Additional scoped checks:

```powershell
.\.venv-ci\Scripts\python.exe -m pytest -q software/tests/unit/test_zero_write_waveshare_adapter_v1.py software/tests/unit/test_zero_write_sole_writer_v1.py software/tests/integration/test_zero_write_waveshare_contract_v1.py software/ai/tests/test_translation_weighted_evidence.py
```

Result: **36 passed**, 6.30 seconds. These validate offline controller-preview,
lifecycle/schema behavior, and internal training-evidence consistency. They do
not run inference, retrain models, or qualify a controller or physical task.

The [merged-main hosted run](https://github.com/j-webtek/robot-arm-build/actions/runs/36255399846)
also completed successfully at this SHA, covering Linux/Windows and Python
3.10/3.12. It runs the portable selection, not the additional local selection.

`python scripts/audit_github_snapshot.py` completed with exit 0: 5,678 paths,
903.6 MiB, zero unresolved review findings and 14 reviewed synthetic fixtures.
This is a heuristic snapshot scan, not full-history or security certification.

## Resolved environment

Recorded with `.venv-ci/Scripts/python.exe -m pip freeze`; this is an observation,
not a supported lockfile or a promise of bit-for-bit wheel reproduction:

```text
attrs==26.1.0
colorama==0.4.6
exceptiongroup==1.3.1
iniconfig==2.3.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
packaging==26.3
pillow==12.3.0
pluggy==1.6.0
Pygments==2.21.0
pytest==8.4.2
referencing==0.37.0
rpds-py==0.30.0
tomli==2.4.1
typing_extensions==4.16.0
```

The `rocell` freeze entry is a local file URL pointing to the pinned checkout's
`software` directory, omitted here as a machine-specific path. Its installed
version was 0.1.0. Build-backend isolation dependencies are not captured by freeze.

## Remaining release gates

No tag or downloadable release was created. Publication still needs final
candidate selection, both workstreams' compatibility review, content/license
review, completed release notes, and explicit approval. Native-helper-dependent
tests and physical qualification were not run. See [test tiers](../CI.md) and
the [release checklist](../RELEASING.md).
