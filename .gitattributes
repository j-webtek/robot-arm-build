# Preserve exact bytes: hardware manifests and firmware reviews use hashes.
* -text

# CAD/print packages use ordinary Git in this initial baseline because the
# account's Git LFS budget is unavailable. Review size before adding binaries.
